- $merge:
    - id: min_tasks
      label: Minimum tasks
      type: number
      min: 0
      default: 1
    - $with.min_overrides
- $merge:
    - id: max_tasks
      label: Maximum tasks
      type: number
      min: 1
      default: 3
    - $with.max_overrides
