# A cluster-only service with an allow-list switched on but no sources listed:
# the NetworkPolicy must render and deny every ingress peer.
image:
  repository: 123456789012.dkr.ecr.us-east-1.amazonaws.com/demo-web
  tag: v1
networkPolicy:
  enabled: true
